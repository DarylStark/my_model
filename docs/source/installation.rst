Installation
============

Installation of the package is very straight forward using ``pip``:

.. code-block:: bash

    pip install ds-my-model

We used the name ``ds-my-model`` on PyPI to prevent name clashing with other projects that are named similar.